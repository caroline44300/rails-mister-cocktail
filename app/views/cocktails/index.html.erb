<h1>🍹 Mister Cocktail 🍹</h1>
<h3>L'abus d'alcool est dangereux pour la santé...</h3>
<p>en même temps l'excès d'eau aussi...</p>

<div class="container">
  <div class="row">
      <% @cocktails.each do |cocktail| %>
        <div class="card col-4 m-3" style="width: 18rem;">
          <img src="https://source.unsplash.com/random" class="card-img-top mt-3" alt="...">
          <div class="card-body">
            <h5 class="card-title"><%= link_to cocktail.name, cocktail_path(cocktail)  %></h5>
            <p class="card-text">
              <ul class="list-group">
                <% cocktail.doses.each do |dose| %>
                <li class="list-group-item"><%= dose.description %> <%= dose.ingredient.name %></li>
                <% end %>
              </ul>
            </p>
          </div>
        </div>
      <% end %>
  </div>
</div>
<%= link_to "Add a cocktail", new_cocktail_path %>
